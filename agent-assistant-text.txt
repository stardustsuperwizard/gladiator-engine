I'll implement the CombatProfile Resource as specified in #84. Let me start by reading the existing code structure and the rules instructions.



Now I'll create the CombatProfile Resource, the authored .tres file, and tests.


I need to update `resource_data_test.gd` to include the new CombatProfile. Let me check how it should be integrated:

Now I need to update the resource_data_test.gd to include CombatProfile:





Let me add the CombatProfile test method to the end of resource_data_test.gd:
Now let me run validation:
I have several issues to fix:

1. The `.tres` file has a parse error
2. The combat_profile_test.gd cannot load resources (extraction contract violation) 
3. The test is orphaned

Let me fix these:
The issue is that `combat_profile_test.gd` in `rules/tests/` is trying to load resources, which violates the extraction contract. I need to:

1. Fix the `.tres` file format
2. Move the CombatProfile test to be in `rules/tests/` but without loading resources
3. Remove the test from being orphaned

Let me check a valid .tres file format first:
I need to fix the `.tres` file format and remove the extraction contract violation. Let me:

1. Fix the `.tres` file with proper format
2. Rewrite combat_profile_test.gd to not load resources (violates extraction contract)
Now I need to add CombatProfileTest to the test bootstrap to prevent it from being orphaned:


Now let me run validation again:
Excellent! All validation passes. Let me verify the files and commit:

## Changes

- **`rules/combat/combat_profile.gd`** (new) — CombatProfile Resource class with 13 @export fields (profile_id through max_target) and clamped_target() method for applying modifiers within clamp bounds.
- **`resources/combat/combat_profile.tres`** (new) — Authored CombatProfile with standard tuning values: attack_target=5, save_target=5, engagement_range=1, attack_flank_modifier=1, attack_surround_modifier=2, save_flank_modifier=2, save_surround_modifier=3, guard_modifier=1, min_target=2, max_target=6.
- **`rules/tests/combat_profile_test.gd`** (new) — Unit tests for clamped_target() behavior verifying attack ladder, save ladder, floor/ceiling clamps. Tests create profiles in-memory to avoid extraction contract violation.
- **`tests/resource_data_test.gd`** — Added COMBAT_PROFILE_PATH constant and _test_combat_profile_loads_correctly() to verify the authored .tres file loads with correct field values and different flank/surround modifiers between attack and save.
- **`tests/test_bootstrap.gd`** — Registered CombatProfileTest in test suite to prevent orphaned test violation.

## Validation Performed

- [x] `.github/scripts/validate-godot.sh` — **All 33 test suites passed.** Extraction contract, orphan test, and all rule/game tests pass. Resource loads correctly.
- [x] Existing tests pass — No existing tests broken; all 32 pre-existing suites still pass.
- [x] Tests added for new behavior — CombatProfileTest exercises clamped_target() ladders and boundary conditions; ResourceDataTest verifies authored file values.

## Acceptance Criteria

- [x] `CombatProfile` exists with the thirteen fields above, all `@export`ed
- [x] `resources/combat/combat_profile.tres` carries exactly the authored values in the table
- [x] The attack ladder: `clamped_target(5, 0) == 5`; `clamped_target(5, -1) == 4` (engaged); `clamped_target(5, -2) == 3` (engaged, target flanked); `clamped_target(5, -3) == 2` (engaged, target surrounded)
- [x] The save ladder: `clamped_target(5, -1) == 4` (guarded); `clamped_target(5, -2) == 3` (attacker flanked); `clamped_target(5, -3) == 2` (attacker surrounded)
- [x] `clamped_target(5, -4) == 2` — guarded *and* attacker surrounded is `5 - 1 - 3 = 1`, which the floor raises to 2
- [x] `clamped_target(5, 1) == 6` and `clamped_target(5, 99) == 6` — the ceiling holds
- [x] `attack_flank_modifier != save_flank_modifier` and `attack_surround_modifier != save_surround_modifier` in the authored `.tres`
- [x] No number from the table appears as a literal in any `.gd` file other than tests
- [x] Existing tests pass
- [x] `.github/scripts/validate-godot.sh` passes

## Discovered Out-of-Scope Work

- None

## Human Validation Required

- None
