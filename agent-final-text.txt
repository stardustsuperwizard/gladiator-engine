## Changes

- **rules/fighters/fighter_template.gd** — Added `range_hexes`, `attack`, `damage`, and `ability_tags` as `@export` fields; added `has_ability_tag()` method for exact string matching; removed `point_value` field
- **rules/fighters/fighter.gd** — Added `range_hexes()`, `attack()`, `damage()`, and `ability_tags()` stat readers that return template values; removed `point_value()` method; updated class docstring comment count from five to nine stat readers
- **resources/fighters/warrior.tres** — Set `range_hexes=1`, `attack=3`, `damage=2`, `ability_tags=["cleave"]`; removed `point_value`
- **resources/fighters/archer.tres** — Set `range_hexes=4`, `attack=2`, `damage=1`, `ability_tags=[]`; removed `point_value`
- **rules/tests/fighter_template_test.gd** — Removed `point_value` assertions; added new stat defaults and round-trip tests
- **rules/tests/fighter_test.gd** — Updated test constants to match new stats; removed `point_value` comparisons; added assertions for new stat readers
- **rules/tests/fighter_serialization_test.gd** — No changes required; `to_dict()` unchanged as per architecture constraint
- **rules/tests/attack_action_test.gd** — Removed `point_value` assignment from fixture builder
- **tests/resource_data_test.gd** — Updated authored value assertions and round-trip tests to verify new stats; removed `point_value` checks

## Validation Performed

- [x] `.github/scripts/validate-godot.sh` — All 32 test suites passed. Godot headless validation passed.
- [x] Existing tests pass — Fighter Template Test, Fighter Test, Fighter Serialization Test, Resource Data Test, Attack Action Test all passing
- [x] Tests added for new behavior — New stat defaults and round-trip serialization tested; `ability_tags()` copy isolation tested

## Acceptance Criteria

- [x] `FighterTemplate` exposes `range_hexes`, `attack`, `damage` and `ability_tags`, all `@export`ed
- [x] `FighterTemplate.has_ability_tag()` matches by exact string, and returns `false` for a tag not present
- [x] `Fighter.range_hexes()`, `.attack()`, `.damage()` and `.ability_tags()` return the template's values
- [x] `Fighter.ability_tags()` returns a copy — appending to the returned array does not change the template
- [x] `grep -rn "point_value" rules/ resources/ tests/` returns nothing
- [x] `warrior.tres` and `archer.tres` carry exactly the values in the table
- [x] `Fighter.to_dict()` output is byte-identical to before this change, for a fighter in the same state
- [x] Existing tests pass
- [x] `.github/scripts/validate-godot.sh` passes

## Discovered Out-of-Scope Work

None

## Human Validation Required

None
